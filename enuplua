--[[
    TAKA HUB | GARDEN HORIZONS
    by Kiyotakaa
    PART 1: Setup & Main/Game Tab
]]

-- 1. WAIT FOR GAME TO LOAD
repeat task.wait() until game:IsLoaded()

-- 1.5 DEFINE SERVICES
local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")

-- 2. NOTIFY USER
game:GetService("StarterGui"):SetCore("SendNotification", {
    Title = "Taka Hub",
    Text = "Loading Interface...",
    Duration = 5,
})

-- 3. LOAD LIBRARY (Fluent UI - Modern & Clean)
local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

-- 4. CREATE WINDOW
local Window = Fluent:CreateWindow({
    Title = "TAKA HUB | Garden Horizons",
    SubTitle = "by Kiyotakaa",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.LeftControl
})

-- 5. CREATE TABS
local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "rbxassetid://10747383819" }),
    Game = Window:AddTab({ Title = "Game", Icon = "rbxassetid://10747383819" }),
    Event = Window:AddTab({ Title = "Event", Icon = "rbxassetid://10747383819" }),
    ESP = Window:AddTab({ Title = "ESP", Icon = "rbxassetid://10747383819" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "rbxassetid://10747383819" })
}

-- ==========================================
-- MAIN TAB (User Info)
-- ==========================================
Tabs.Main:AddParagraph({
    Title = "User Information",
    Content = "Username: " .. Player.Name .. "\nDisplay: " .. Player.DisplayName .. "\nUser ID: " .. Player.UserId .. "\nAccount Age: " .. Player.AccountAge .. " days"
})

Tabs.Main:AddParagraph({
    Title = "Credits",
    Content = "Scripting: Kiyotakaa / Taka Hub Team"
})

Tabs.Main:AddButton({
    Title = "Copy Discord Invite",
    Description = "Join our community",
    Callback = function()
        setclipboard("https://discord.gg/avGM6WQKCx")
        Fluent:Notify({
            Title = "Link Copied!",
            Content = "Discord invite copied to clipboard",
            Duration = 5
        })
    end
})

-- ==========================================
-- GAME TAB (Auto Buy/Sell)
-- ==========================================
local AllSeeds = {"Carrot", "Corn", "Onion", "Strawberry", "Mushroom", "Beetroot", "Tomato", "Apple", "Rose", "Wheat", "Banana", "Plum", "Potato", "Cabbage", "Cherry", "Bamboo", "Mango"}
local BestSeeds = {"Wheat", "Banana", "Plum", "Potato", "Cabbage", "Cherry", "Bamboo", "Mango"}

local SelectedSeeds = {}
local AutoBuyEnabled, BuyAllEnabled, BuyBestEnabled = false, false, false

local ShopCoords = Vector3.new(176.76, 204.01, 674.03)
local SellCoords = Vector3.new(149.08, 204.03, 672.81)

local PurchaseRemote = ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("PurchaseShopItem")
local SellRemote = ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("SellItems")
local ShopGui = Player:WaitForChild("PlayerGui"):WaitForChild("SeedShop")

-- Helper to check stock
local function GetStock(seedName)
    local success, stockLabel = pcall(function()
        return ShopGui.Frame.ScrollingFrame[seedName].MainInfo.StockText
    end)
    if success and stockLabel then
        local stockAmount = string.match(stockLabel.Text, "%d+")
        return tonumber(stockAmount) or 0
    end
    return 0
end

-- Purchase Logic
local function PerformPurchase(seedList, toggleFlag)
    local character = Player.Character
    local hrp = character and character:FindFirstChild("HumanoidRootPart")
    if not hrp or #seedList == 0 then return end

    local itemsToBuy = {}
    for _, name in pairs(seedList) do
        if GetStock(name) > 0 then
            table.insert(itemsToBuy, name)
        end
    end

    if #itemsToBuy > 0 then
        local lastLocation = hrp.CFrame
        hrp.CFrame = CFrame.new(ShopCoords)
        task.wait(0.3)

        for _, name in pairs(itemsToBuy) do
            if not _G[toggleFlag] then break end
            local itemName = name .. " Seed"
            while _G[toggleFlag] and GetStock(name) > 0 do
                PurchaseRemote:InvokeServer("SeedShop", itemName)
                task.wait(0.01)
            end
        end
        hrp.CFrame = lastLocation
    end
end

-- SEED SECTION
Tabs.Game:AddSection("Seed Shop")

Tabs.Game:AddDropdown("SeedSelect", {
    Title = "Select Seeds",
    Values = AllSeeds,
    Multi = true,
    Default = {},
    Callback = function(Value)
        SelectedSeeds = Value
    end
})

Tabs.Game:AddToggle("AutoBuySelected", {
    Title = "Auto-Buy Selected Seeds",
    Default = false,
    Callback = function(Value)
        AutoBuyEnabled = Value
        _G.CustomToggle = Value
        if Value then
            task.spawn(function()
                while AutoBuyEnabled do
                    PerformPurchase(SelectedSeeds, "CustomToggle")
                    task.wait(1)
                end
            end)
        end
    end
})

Tabs.Game:AddToggle("AutoBuyAll", {
    Title = "Auto-Buy ALL Seeds",
    Default = false,
    Callback = function(Value)
        BuyAllEnabled = Value
        _G.AllToggle = Value
        if Value then
            task.spawn(function()
                while BuyAllEnabled do
                    PerformPurchase(AllSeeds, "AllToggle")
                    task.wait(1)
                end
            end)
        end
    end
})

Tabs.Game:AddToggle("AutoBuyBest", {
    Title = "Auto-Buy Best Seeds",
    Default = false,
    Callback = function(Value)
        BuyBestEnabled = Value
        _G.BestToggle = Value
        if Value then
            task.spawn(function()
                while BuyBestEnabled do
                    PerformPurchase(BestSeeds, "BestToggle")
                    task.wait(1)
                end
            end)
        end
    end
})

-- GEAR SECTION
local SelectedGears = {}
local GearAutoBuyEnabled = false
local ShopCFrame = CFrame.new(211.40, 204.01, 608.80)

local function GetGearStockCount()
    local totalStock = 0
    local scrollingFrame = Player.PlayerGui.GearShop.Frame.ScrollingFrame
    for _, gearName in ipairs(SelectedGears) do
        local gearPath = scrollingFrame:FindFirstChild(gearName)
        if gearPath and gearPath.MainInfo:FindFirstChild("StockText") then
            local text = gearPath.MainInfo.StockText.Text
            local stockNum = tonumber(string.match(text, "%d+")) or 0
            if stockNum > 0 then
                totalStock = totalStock + stockNum
            end
        end
    end
    return totalStock
end

local function RunGearOneBlinkBuy()
    local char = Player.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local root = char.HumanoidRootPart
    local scrollingFrame = Player.PlayerGui.GearShop.Frame.ScrollingFrame

    if GetGearStockCount() > 0 then
        local originalPos = root.CFrame
        root.CFrame = ShopCFrame
        task.wait(0.2)

        while GearAutoBuyEnabled and GetGearStockCount() > 0 do
            for _, gearName in ipairs(SelectedGears) do
                local gearPath = scrollingFrame:FindFirstChild(gearName)
                if gearPath then
                    local stockNum = tonumber(string.match(gearPath.MainInfo.StockText.Text, "%d+")) or 0
                    if stockNum > 0 then
                        pcall(function()
                            PurchaseRemote:InvokeServer("GearShop", gearName)
                        end)
                    end
                end
            end
            task.wait(0.1)
        end
        root.CFrame = originalPos
    end
end

Tabs.Game:AddSection("Gear Shop")

Tabs.Game:AddDropdown("GearSelect", {
    Title = "Select Gears",
    Values = {"Watering Can", "Basic Sprinkler", "Harvest Bell", "Turbo Sprinkler", "Favorite Tool", "Super Sprinkler", "Trowel"},
    Multi = true,
    Default = {},
    Callback = function(Value)
        SelectedGears = Value
    end
})

Tabs.Game:AddToggle("AutoBuyGear", {
    Title = "Auto Buy Gears",
    Default = false,
    Callback = function(Value)
        GearAutoBuyEnabled = Value
        if Value then
            task.spawn(function()
                while GearAutoBuyEnabled do
                    RunGearOneBlinkBuy()
                    task.wait(0.5)
                end
            end)
        end
    end
})

-- SELL SECTION
Tabs.Game:AddSection("Sell System")

Tabs.Game:AddButton({
    Title = "Sell Holding",
    Description = "Teleport & Sell Hand",
    Callback = function()
        local character = Player.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        local equippedItem = character and character:FindFirstChildOfClass("Tool")

        if hrp and equippedItem then
            local lastLocation = hrp.CFrame
            hrp.CFrame = CFrame.new(SellCoords)
            task.wait(0.3)
            pcall(function() SellRemote:InvokeServer("SellSingle") end)
            hrp.CFrame = lastLocation
        else
            Fluent:Notify({
                Title = "Action Failed",
                Content = "Ensure you are holding a plant!",
                Duration = 2
            })
        end
    end
})

Tabs.Game:AddButton({
    Title = "Sell All",
    Description = "Teleport & Sell All",
    Callback = function()
        local character = Player.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local lastLocation = hrp.CFrame
            hrp.CFrame = CFrame.new(SellCoords)
            task.wait(0.3)
            pcall(function() SellRemote:InvokeServer("SellAll") end)
            hrp.CFrame = lastLocation
        end
    end
})

-- PLAYER SETTINGS
Tabs.Game:AddSection("Player Settings")

local InfJumpEnabled = false
UserInputService.JumpRequest:Connect(function()
    if InfJumpEnabled then
        local character = Player.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end
end)

Tabs.Game:AddToggle("InfJump", {
    Title = "Infinite Jump",
    Default = false,
    Callback = function(Value)
        InfJumpEnabled = Value
    end
})

local InstantInteractEnabled = false
local ProximityPromptService = game:GetService("ProximityPromptService")

local function instantPrompt(prompt)
    if InstantInteractEnabled and prompt:IsA("ProximityPrompt") then
        prompt.HoldDuration = 0
    end
end

for _, obj in ipairs(Workspace:GetDescendants()) do
    instantPrompt(obj)
end

Workspace.DescendantAdded:Connect(function(obj)
    instantPrompt(obj)
end)

Tabs.Game:AddToggle("InstantInteract", {
    Title = "Instant Interact",
    Default = false,
    Callback = function(Value)
        InstantInteractEnabled = Value
        if Value then
            for _, obj in ipairs(Workspace:GetDescendants()) do
                instantPrompt(obj)
            end
        else
            for _, obj in ipairs(Workspace:GetDescendants()) do
                if obj:IsA("ProximityPrompt") then
                    obj.HoldDuration = 0.5
                end
            end
        end
    end
})

local AntiAfkEnabled = false
Player.Idled:Connect(function()
    if AntiAfkEnabled then
        VirtualUser:Button2Down(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0,0), Workspace.CurrentCamera.CFrame)
    end
end)

Tabs.Game:AddToggle("AntiAfk", {
    Title = "Anti-AFK",
    Default = false,
    Callback = function(Value)
        AntiAfkEnabled = Value
        if Value then
            Fluent:Notify({
                Title = "Anti-AFK Active",
                Content = "Inactivity kick has been disabled",
                Duration = 3
            })
        end
    end
})

-- ==========================================
-- EVENT TAB (Botanist)
-- ==========================================
local BotanistCoords = Vector3.new(213.43, 204.00, 621.11)
local BotanistRemote = ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("BotanistQuestRequest")

local QuestInfo = Tabs.Event:AddParagraph({
    Title = "Current Quest",
    Content = "None/Loading"
})

task.spawn(function()
    while true do
        local success, questText = pcall(function()
            return Workspace.MapPhysical.Botanist.Botanist.Head.OverheadSpeech.CONTENT.Text
        end)
        if success and questText and questText ~= "" and questText ~= " " then
            QuestInfo:SetContent("Current Quest: " .. questText)
        end
        task.wait(1.5)
    end
end)

Tabs.Event:AddButton({
    Title = "Teleport to Botanist",
    Description = "Teleport to Quest NPC",
    Callback = function()
        local character = Player.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(BotanistCoords)
        end
    end
})

Tabs.Event:AddButton({
    Title = "Turn In Single (Holding)",
    Callback = function()
        local character = Player.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        local holding = character and character:FindFirstChildOfClass("Tool")

        if hrp then
            if holding then
                local lastPos = hrp.CFrame
                hrp.CFrame = CFrame.new(BotanistCoords)
                task.wait(0.5)
                BotanistRemote:InvokeServer("TurnInSingle")
                task.wait(0.2)
                hrp.CFrame = lastPos
            else
                Fluent:Notify({
                    Title = "Item Required",
                    Content = "Please hold a plant first!",
                    Duration = 3
                })
            end
        end
    end
})

Tabs.Event:AddButton({
    Title = "Turn In All (Best Match)",
    Callback = function()
        local character = Player.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local lastPos = hrp.CFrame
            hrp.CFrame = CFrame.new(BotanistCoords)
            task.wait(0.5)
            BotanistRemote:InvokeServer("TurnInAll")
            task.wait(0.2)
            hrp.CFrame = lastPos
        end
    end
    })

-- ==========================================
-- ESP TAB
-- ==========================================
local ActiveObjects = {}
local Mutation_ESP_Enabled = false
local Weight_ESP_Enabled = false

local function ClearESP(obj)
    if ActiveObjects[obj] then
        for _, v in pairs(ActiveObjects[obj]) do
            if v then v:Destroy() end
        end
        ActiveObjects[obj] = nil
    end
end

local function ApplyCombinedESP(target, labelText, isMutation)
    if ActiveObjects[target] then
        local billboard = ActiveObjects[target][2]
        if billboard and billboard:FindFirstChild("TextLabel") then
            billboard.TextLabel.Text = labelText
        end
        return
    end

    local highlight = Instance.new("Highlight")
    highlight.Adornee = target
    highlight.FillColor = isMutation and Color3.fromRGB(255, 255, 0) or Color3.fromRGB(0, 255, 255)
    highlight.FillTransparency = 0.5
    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
    highlight.Parent = target

    local billboard = Instance.new("BillboardGui")
    billboard.Size = UDim2.new(0, 250, 0, 50)
    billboard.AlwaysOnTop = true
    billboard.ExtentsOffset = Vector3.new(0, 0, 0)
    billboard.Adornee = target
    billboard.Parent = target

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextStrokeTransparency = 0.3
    label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    label.Font = Enum.Font.GothamBold
    label.TextSize = 12
    label.Parent = billboard
    
    ActiveObjects[target] = {highlight, billboard}
end

local SelectedMutations = {}
local SelectedPlantTypes = {}

local function ProcessCombined(obj, displayName)
    local cleanName = displayName:gsub("%d+", "")

    local plantMatched = false
    if #SelectedPlantTypes == 0 or table.find(SelectedPlantTypes, "All") then
        plantMatched = true
    else
        for _, p in pairs(SelectedPlantTypes) do
            if string.find(cleanName:lower(), p:lower()) then plantMatched = true break end
        end
    end

    if not plantMatched then
        ClearESP(obj)
        return
    end

    local displayTraits = {}
    local mutMatched = false

    if Mutation_ESP_Enabled then
        local mutAttr = tostring(obj:GetAttribute("Mutation") or ""):lower()
        local ripeAttr = tostring(obj:GetAttribute("RipenessStage") or ""):lower()
        local varAttr = tostring(obj:GetAttribute("Variant") or ""):lower()

        for _, filterName in pairs(SelectedMutations) do
            local f = filterName:lower()
            if (mutAttr ~= "" and string.find(mutAttr, f)) or
                (ripeAttr ~= "" and string.find(ripeAttr, f)) or
                (varAttr ~= "" and string.find(varAttr, f)) then
                if not table.find(displayTraits, filterName) then
                    table.insert(displayTraits, filterName)
                end
                mutMatched = true
            end
        end
    end

    local weightAttr = obj:GetAttribute("PlantWeight") or obj:GetAttribute("FruitWeight")
    local weightMatched = Weight_ESP_Enabled and weightAttr ~= nil

    if mutMatched or weightMatched then
        local finalLabel = "[" .. cleanName .. "]: "
        if mutMatched and #displayTraits > 0 then
            finalLabel = finalLabel .. "[" .. table.concat(displayTraits, ",") .. "] "
        end
        if weightMatched then
            local rounded = math.floor(tonumber(weightAttr) * 100 + 0.5) / 100
            finalLabel = finalLabel .. "[" .. tostring(rounded) .. " kg]"
        end
        ApplyCombinedESP(obj, finalLabel, mutMatched)
    else
        ClearESP(obj)
    end
end

task.spawn(function()
    while true do
        if Mutation_ESP_Enabled or Weight_ESP_Enabled then
            local plantsFolder = Workspace:FindFirstChild("ClientPlants")
            if plantsFolder then
                for _, item in pairs(plantsFolder:GetChildren()) do
                    local fruits = {}
                    for _, child in pairs(item:GetDescendants()) do
                        if string.find(child.Name, "Fruit") then
                            table.insert(fruits, child)
                        end
                    end
                    if #fruits > 0 then
                        for _, fruitPart in ipairs(fruits) do
                            ProcessCombined(fruitPart, item.Name)
                        end
                    else
                        ProcessCombined(item, item.Name)
                    end
                end
            end
        else
            for o, _ in pairs(ActiveObjects) do ClearESP(o) end
        end
        task.wait(0.2)
    end
end)

Tabs.ESP:AddSection("ESP Controls")

Tabs.ESP:AddToggle("MutationESP", {
    Title = "Enable Mutation ESP",
    Default = false,
    Callback = function(Value)
        Mutation_ESP_Enabled = Value
        if not Value then for o, _ in pairs(ActiveObjects) do ClearESP(o) end end
    end
})

Tabs.ESP:AddToggle("WeightESP", {
    Title = "Enable Weight ESP",
    Default = false,
    Callback = function(Value)
        Weight_ESP_Enabled = Value
        if not Value then for o, _ in pairs(ActiveObjects) do ClearESP(o) end end
    end
})

Tabs.ESP:AddDropdown("PlantTypes", {
    Title = "Select Target Plant Types",
    Values = {"All", "Carrot", "Corn", "Onion", "Strawberry", "Mushroom", "Beetroot", "Tomato", "Apple", "Rose", "Wheat", "Banana", "Plum", "Potato", "Cabbage", "Cherry", "Bamboo", "Mango", "Dawnfruit", "Dawnblossom", "Sunpetal", "Dandelion", "Bellpepper", "Goldenberry", "Amberpine", "Emberwood", "Pomegrante", "Birch", "Orange", "Olive"},
    Multi = true,
    Default = {"All"},
    Callback = function(Value)
        SelectedPlantTypes = Value
        for o, _ in pairs(ActiveObjects) do ClearESP(o) end
    end
})

Tabs.ESP:AddDropdown("Mutations", {
    Title = "Filter Specific Mutations",
    Values = {"Foggy", "Flooded", "Soaked", "Chilled", "Snowy", "Sand", "Tidal", "Frostbit", "Mossy", "Shocked", "Muddy", "Galactic", "Meteoric", "Nova", "Starstruck", "Unripe", "Ripened", "Lush", "Silver", "Gold"},
    Multi = true,
    Default = {},
    Callback = function(Value)
        SelectedMutations = Value
        for o, _ in pairs(ActiveObjects) do ClearESP(o) end
    end
})

-- ==========================================
-- AUTO COLLECT SECTION
-- ==========================================
Tabs.ESP:AddSection("Auto Collect")

local AutoCollect_Enabled = false
local Collect_Plants = {}
local Collect_Mutations = {}
local Collect_MinWeight = 0

local HarvestRemote = ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("HarvestFruit")

Tabs.ESP:AddToggle("AutoCollect", {
    Title = "Enable Auto Collect",
    Default = false,
    Callback = function(Value)
        AutoCollect_Enabled = Value
    end
})

Tabs.ESP:AddDropdown("CollectPlants", {
    Title = "Target Plant Types",
    Values = {"All", "Carrot", "Corn", "Onion", "Strawberry", "Mushroom", "Beetroot", "Tomato", "Apple", "Rose", "Wheat", "Banana", "Plum", "Potato", "Cabbage", "Cherry", "Bamboo", "Mango", "Dawnfruit", "Dawnblossom", "Sunpetal", "Dandelion", "Bellpepper", "Goldenberry", "Amberpine", "Emberwood", "Pomegrante", "Birch", "Orange", "Olive"},
    Multi = true,
    Default = {},
    Callback = function(Value)
        Collect_Plants = Value
    end
})

Tabs.ESP:AddDropdown("CollectMutations", {
    Title = "Target Mutations/Variants",
    Values = {"Foggy", "Flooded", "Soaked", "Chilled", "Snowy", "Sand", "Tidal", "Frostbit", "Mossy", "Shocked", "Muddy", "Galactic", "Meteoric", "Nova", "Starstruck", "Unripe", "Ripened", "Lush", "Silver", "Gold"},
    Multi = true,
    Default = {},
    Callback = function(Value)
        Collect_Mutations = Value
    end
})

Tabs.ESP:AddSlider("MinWeight", {
    Title = "Min Weight Threshold (0 = Off)",
    Description = "Minimum weight to collect",
    Default = 0,
    Min = 0,
    Max = 10,
    Rounding = 2,
    Callback = function(Value)
        Collect_MinWeight = Value
    end
})

local function HarvestTarget(targetObj, parentName)
    local p = game.Players.LocalPlayer
    local char = p.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end

    local plantUuid = targetObj:GetAttribute("Uuid")
    if not plantUuid then return end

    local anchor = targetObj:FindFirstChild("PlantAnchor") or targetObj:FindFirstChild("FruitAnchor")
    if not anchor and targetObj.Parent then
        anchor = targetObj.Parent:FindFirstChild("PlantAnchor") or targetObj.Parent:FindFirstChild("FruitAnchor")
    end

    if anchor then
        char.HumanoidRootPart.CFrame = CFrame.new(anchor.Position + Vector3.new(0, 1.5, 2), anchor.Position)
        task.wait(0.1)
        local args = {
            {
                {
                    Uuid = plantUuid
                }
            }
        }
        HarvestRemote:FireServer(unpack(args))
        task.wait(0.2)
    end
end

local function ProcessCollect(obj, parentName)
    if obj:GetAttribute("FullyGrown") ~= true then return end
    if obj:GetAttribute("Favorited") == true then return end

    local currentWeight = obj:GetAttribute("FruitWeight") or obj:GetAttribute("PlantWeight") or 0
    if Collect_MinWeight > 0 and currentWeight < Collect_MinWeight then return end

    local cleanName = parentName:gsub("%d+", "")

    local typeMatch = table.find(Collect_Plants, "All")
    if not typeMatch then
        for _, p in pairs(Collect_Plants) do
            if string.find(cleanName:lower(), p:lower()) then typeMatch = true break end
        end
    end
    if not typeMatch then return end

    local mutationCount = 0
    for _ in pairs(Collect_Mutations) do mutationCount = mutationCount + 1 end

    if mutationCount == 0 then
        HarvestTarget(obj, parentName)
    else
        local mut = tostring(obj:GetAttribute("Mutation") or ""):lower()
        local ripe = tostring(obj:GetAttribute("RipenessStage") or ""):lower()
        local var = tostring(obj:GetAttribute("Variant") or ""):lower()

        local mutMatched = false
        for _, selected in pairs(Collect_Mutations) do
            local f = selected:lower()
            if (mut ~= "" and string.find(mut, f)) or
                (ripe ~= "" and string.find(ripe, f)) or
                (var ~= "" and string.find(var, f)) then
                mutMatched = true
                break
            end
        end

        if mutMatched then
            HarvestTarget(obj, parentName)
        end
    end
end

task.spawn(function()
    while true do
        if AutoCollect_Enabled then
            local plants = Workspace.ClientPlants:GetChildren()
            for _, item in pairs(plants) do
                if not AutoCollect_Enabled then break end
                local fruitsFound = false
                for _, child in pairs(item:GetChildren()) do
                    if string.find(child.Name, "Fruit") then
                        fruitsFound = true
                        ProcessCollect(child, item.Name)
                    end
                end
                if not fruitsFound then
                    ProcessCollect(item, item.Name)
                end
            end
        end
        task.wait(0.5)
    end
end)

-- ==========================================
-- SETTINGS TAB
-- ==========================================
Tabs.Settings:AddButton({
    Title = "Destroy UI",
    Description = "Close the hub",
    Callback = function()
        Window:Destroy()
    end
})

Tabs.Settings:AddParagraph({
    Title = "Taka Hub V1.0",
    Content = "Made by Kiyotakaa\nThanks for using!"
})

-- INITIAL NOTIFICATION
Fluent:Notify({
    Title = "Taka Hub Loaded",
    Content = "Welcome, " .. Player.DisplayName .. "!",
    Duration = 5
})
